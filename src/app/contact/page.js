import { Contact as ContactContainer } from '@/components'

export const metadata = {
  title: 'Contact - Rashid Machingal',
  description: 'Contact is a report automation service where I converted Figma UI designs to React components for both the web and landing page. View screenshots of the project here.',
}

export default function Contact() {
  return <ContactContainer/>
}
